/////LEFT ROTATE
/*
 class LeftRotate{
 constructor(arr,num){
 this.num = num;
 this.arr = arr;

 this.leftSort = function(){
 return num === 0 ? arr.slice(0) : arr.slice(num).concat(arr.slice(0,num)).toString().replace(/,/g," ");
 }
 }
 }

 let a = new LeftRotate([1,2,3,4,5],2);

 console.log(a.leftSort());*/